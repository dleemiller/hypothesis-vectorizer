# Test the GEPA-tuned GeneratePool instruction at the -l encoder (vs hand-written instruction).
run_name: trec_tuned_l
seed: 7
data: {name: trec}
encoder: {model: dleemiller/finecat-nli-l}
lm: {instruction_path: models/proposer_instruction.json}
